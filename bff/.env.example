# 服务端口（改为 3001，避免与 OrbitAgent 3000 冲突）
PORT=3001

# ---------- OrbitAgent 六爻服务 ----------
AGENT_API_URL=http://127.0.0.1:3000
# 开发环境无需手动配置 Key，BFF 会自动通过 /dev/token 获取 JWT
AGENT_API_KEY=
AGENT_TIMEOUT_MS=60000
