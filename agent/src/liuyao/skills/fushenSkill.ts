/**
 * 5.13 FuShen Skill — 伏神 / 飞神 (P2, optional). Stub.
 */
import type { FuShenSkillInput, FuShenSkillOutput } from '../types/skill';
import { todo } from '../constants/hexagrams';

export function fushenSkill(_input: FuShenSkillInput): FuShenSkillOutput {
  todo('5', 'fushenSkill: 伏神 / 飞神 (P2)');
  return { hiddenGods: [] };
}
